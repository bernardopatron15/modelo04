
<%- include('./cabecalho') %>


<!-- SECTION -->
<div class="section">
  <div class="container">
    <div class="row">
      <!-- STORE -->
      <div id="store" class="col-md-9">
        <div class="row">
          <!-- Products loop -->
          <% produtos.forEach(product => { %>
            <div class="col-md-4 col-xs-6">
              <div class="product">
                <div class="product-img">
                  <img src="/fotos/<%= product.foto %>" alt="<%= product.titulo %>" />
                  <% if (true) { %>
                    <div class="product-label">
                      <span class="sale">-<%= product.desconto %>%</span>
                      <span class="new">Novo</span>
                    </div>
                  <% } %>
                </div>
                <div class="product-body">
                  <p class="product-category"><%= product.categoria.titulo %></p>
                  <h3 class="product-name">
                    <a href="#"><%= product.titulo %></a>
                  </h3>
                  <h4 class="product-price">
                    R$ <%= product.preco.toFixed(2) %> <del class="product-old-price">R$ <%= product.precoantigo.toFixed(2) %></del>
                  </h4>
                </div>
                <div class="add-to-cart">
                  <a href="/produto/<%= product._id %>">
                    <button class="add-to-cart-btn">
                      <i class="fa fa-shopping-cart"></i> COMPRAR AGORA
                    </button>
                  </a>
                </div>
              </div>
            </div>
          <% }); %>
          <!-- End products loop -->
        </div>
      </div>
      <!-- /STORE -->
    </div>
  </div>
</div>
<!-- /SECTION -->

<%- include('./rodape2') %>
